# Session 2026-06-02

## Themen
- Projekt-Setup: `opencode-semantic-anchors`
- arc42-Design-Dokument: Sektionen 1 und 2

## Erledigt

### Projekt-Struktur
- Verzeichnis `~/work/opencode-semantic-anchors/` angelegt
- `PLAN.md` (Phasen 1-5) erstellt
- `MASTER-TODO.md` mit abhakbaren Aufgaben erstellt
- `SESSIONS/`-Ordner angelegt
- `docs/concepts/` und `docs/decisions/` angelegt

### Design-Dokument (arc42) – Fortsetzung

**Section 3: System Scope and Context**
- System Boundary (In/Out-Scope) mit Take before Buy before Make-Check
- Business Context als C4-Diagramm
- Technical Context mit Interface-Tabelle
- Existing Solutions-Check dokumentiert (agentcontract/spec, Kiros, SpecAnchor, etc.)
- **Position im Semantic-Anchors-Ökosystem** – Plugin als opencode-spezifische Integration, analog zu Claude Code Plugin, Cursor Rules etc.
- Alle Quellen mit vollständigen URLs

**Section 4: Solution Strategy**
- 3-Layer-Architektur (Config → Rule Engine → Hooks) mit Diagramm
- 6 Key Technical Decisions:
  1. Enforcement via `tool.execute.before` (nicht `permission.ask`, wegen Regression #7006/#28066)
  2. Structural Coupling Contract statt direkter Anchor-Enforcement
  3. YAML-Config (angelehnt an agentcontract/spec)
  4. Take before Buy before Make
  5. Source Anchor als Architektur-Prinzip
  6. Markdown für Design-Doku (nicht AsciiDoc)
- Quality Goal Realization-Tabelle
- **Alle Quellen mit vollständigen URLs, Titeln und Autoren**

**Section 5: Building Block View**
- Whitebox-Übersicht (Mermaid-Diagramm)
- Plugin Entry Point + Config Layer (loader.ts, schema.ts mit Zod)
- Rule Engine (engine.ts, matcher.ts, presets.ts)
- Hook Handlers + Custom Tools
- Sequence Diagram: Tool Execution (Blocking)
- **Terminologie-Konsolidierung:** `StructuralCouplingContract` (einheitlich statt vorher `ContractRule`/`StructuralCouplingContract`-Mix)
- **Typ-Fixes:** `presets` = `Record<string, string[]>` (IDs), RuleEngine mit `getActiveContracts()` + `incrementOverride()`
- **Fail-Open Prinzip** bei Hook-Fehlern

**Section 6: Runtime View**
- Startup Sequence (mit Config-Validation-Fork)
- Tool Execution: Blocked Path (Step Confirmation)
- Tool Execution: Warn Path (Source Anchor)
- Chat Message Flow (sanfte Reminder)
- Agent Role Change (software-architect ↔ software-developer)
- Error Scenarios (Config-Error, Hook-Error, Bypass max)
- Session Reset

### Konsistenz-Checks
- Querverweise zwischen allen Sections geprüft
- Terminologie `StructuralCouplingContract` durchgängig
- Typdefinitionen (Zod ↔ Interfaces) abgeglichen
- `maxOverrides` in SessionState ergänzt
- Keine `ContractRule`-Altlasten mehr
- Entscheidung: `.md` für lokale Doku, `.adoc` nur bei Contribution

## Nächste Schritte (nach Kochpause)
- Sections 7-11 (Deployment View, Crosscutting Concepts, Design Decisions, Quality Requirements, Glossary)
- ODER: Review und Genehmigung des bestehenden Dokuments
- Take before Buy before Make-Check vor Implementierung: agentcontract/spec genauer prüfen

### Stakeholder-Erweiterung
- Compliance / Governance Officer
- Ethics Reviewer
- Data Privacy Officer
- C-Level / Decision Maker
- Works Council (Betriebsrat)
- Alle liegen außerhalb der direkten Developer-Chain (kein Reporting, brauchen Audit-Logs/Reports)

### Begriffsklärung Anchor vs Contract
- Semantic Anchor: LLM kennt es aus Training (shared vocabulary)
- Semantic Contract / Structural Coupling Contract: Projekt-lokal, maschinell durchsetzbar
- Quelle: LLM-Coding/Semantic-Anchors (about.adoc, rejected-proposals.adoc, Issue #370, Issue #518)

### Wichtige Funde
- Issue #518 von JensGrote: "Structural Coupling Contract + System-Theoretic Semantic Anchors framework"
  - Rename-Vorschlag: Semantic Contract → Structural Coupling Contract
  - Tetrad-Struktur: Technical + Systemic + Human + Contracts
- `agentcontract/spec` auf GitHub: vollständige Spezifikation für Agent Contracts (YAML, pre/post conditions)
- Semantic-Anchors Repo hat Issue #521: "Reposition semantic-anchor-onboarding skill → Semantic Contracts Installer"

## Nächste Schritte
- Section 3 (System Scope and Context) schreiben
- "Take before Buy before Make" durch den restlichen Baum ziehen (Sections 3-11)
- Entscheidung: Wie stark an `agentcontract/spec` anlehnen? (Schema, Terminologie)

---

### Session-Fortsetzung: Verification Section 6

**Section 6: Runtime View** geschrieben (Startup, Blocked/Warn/Error-Pfade, Role-Change, Bypass, Session Reset, 6 Szenarien als Mermaid-Sequence-Diagramme). Anschließend **Verifikation** gegen Sections 1-5 durchgeführt:

**✅ Konsistent:**
- `ConfigLoader::getActiveContracts(role)` — Interface (S5 L98) matches Startup-Sequence (S6)
- `Verdict`-Typ (`allow`, `contract`, `message`, `overrideTool`) — S5 L166-171 matches alle Szenarien
- `SessionState` (`role`, `toolCallCount`, `overrideCount`, `maxOverrides`) — S5 L173-179 matches
- `incrementOverride()` returns `number` — S5 L162 matches S6 L58
- `anchor-bypass` Tool-Name durchgängig
- Bypass Max Overrides Check konsistent
- Role-Resets `toolCallCount` bei Agent-Role-Change
- Config-Validierung → Fallback zu Defaults

**❌ Inkonsistenzen gefunden:**
1. **Fehlende "Error Handling"-Section in 05-building-block-view.md** — S6 L165 referenziert "05-building-block-view.md Section Error Handling", die es nicht gibt. Section 5 hat nur eine Error-Zeile in `bypass.ts`.
2. **`contract: "step-confirmation"` als String im Diagramm (kosmetisch)** — S5 L168 definiert `contract: StructuralCouplingContract | null` (Objekt), S6 L52 zeigt vereinfacht als String. Akzeptabel für Diagramme, aber notiert.

**Entscheidung (ausstehend):** Fix #1 anwenden (Error-Handling-Section in Section 5 ergänzen)? User ist kochen gegangen.

**Stand:** Section 6 geschrieben und verifiziert. Zwei Inkonsistenzen dokumentiert. Wird in nächster Session behoben.
